% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_geweke.R
\name{bootstrap_geweke}
\alias{bootstrap_geweke}
\title{Bootstrap Geweke Diagnostic}
\arguments{
\item{x}{An mcmc.list object where each list element corresponds to an MCMC chain,
and within each chain, there are multiple parameters.}

\item{B}{Integer. The number of bootstrap samples to be generated. Default is 1000.}

\item{n}{Integer. The length of each bootstrap sample. Default is the combined length of all chains for each parameter.}

\item{confidence_level}{Numeric. The confidence level for calculating confidence intervals of Z-scores. Default is 0.95.}

\item{frac1}{Numeric. The proportion of the chain used for the initial segment. Default is 0.1 (the first 10\% of the chain).}

\item{frac2}{Numeric. The proportion of the chain used for the last segment. Default is 0.5 (the last 50\% of the chain).}
}
\value{
A list containing the summary statistics for each parameter, including:
\itemize{
\item z_mean: The mean of the Z-scores.
\item z_sd: The standard deviation of the Z-scores.
\item confidence_intervals: Confidence intervals for the Z-scores.
}
}
\description{
This function performs a bootstrap-based Geweke diagnostic on an MCMC object.
It computes Z-scores for the Geweke diagnostic, allowing for confidence intervals
based on the bootstrap samples.
}
\examples{
\donttest{
library(coda)
mcmc_chain <- coda::as.mcmc.list(coda::mcmc(rnorm(1000)))  # Example MCMC chain
result <- bootstrap_geweke(mcmc_chain, B = 1000, n = 1000, confidence_level = 0.95)
summary(result)
plot(result)
}

}
